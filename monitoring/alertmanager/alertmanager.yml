route:
  receiver: "incident-webhook"
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2m

receivers:
  - name: "incident-webhook"
    webhook_configs:
      - url: "http://incident-webhook:5000/alert"
        send_resolved: true
